[pytest]
testpaths = tests
markers =
    gpu: requires the mlgenn conda env (PyGeNN/mlGeNN, GeNN CPU or CUDA backend) --
        run via "C:\Users\user\miniconda3\envs\mlgenn\python.exe -m pytest -m gpu".
        Not runnable from the plain project .venv.
